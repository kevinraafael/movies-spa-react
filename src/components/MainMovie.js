import React from "react";
import './MainMovie.css'

export default ({ item }) => {
    console.log('to no main movie');
    console.log(item);
    return (
        <section className="MainMovie">

        </section>
    )
}