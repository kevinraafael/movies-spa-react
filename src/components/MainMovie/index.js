import React from "react";
import './index.css'

export default ({ item }) => {
    console.log('to no main movie');
    console.log(item);
    return (
        <section className="MainMovie">

        </section>
    )
}